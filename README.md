
  # LinkedIn Analyzer Prototype

  This is a code bundle for LinkedIn Analyzer Prototype. The original project is available at https://www.figma.com/design/kyZl4LLIomVJi9bOw9qs2L/LinkedIn-Analyzer-Prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  